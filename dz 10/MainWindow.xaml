<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TXT Viewer" Height="450" Width="800">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопка -->
        <Button Content="Открыть TXT файл"
                Width="180"
                Height="40"
                HorizontalAlignment="Left"
                Click="Button_Click"/>

        <!-- Поле вывода текста -->
        <TextBox Grid.Row="1"
                 Name="textBox"
                 Margin="0,10,0,0"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 IsReadOnly="True"/>
    </Grid>
</Window>